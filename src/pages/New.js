import React from 'react';
import NewContainer from '../container/NewContainer';
const New = () => {
  return (
    <section className="new">
      <NewContainer />
    </section>
  );
};

export default New;
